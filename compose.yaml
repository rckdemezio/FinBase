services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: finbase
      MYSQL_USER: finbase
      MYSQL_PASSWORD: finbase
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3307:3306"
    volumes:
      - finbase_mysql_data:/var/lib/mysql

volumes:
  finbase_mysql_data:
